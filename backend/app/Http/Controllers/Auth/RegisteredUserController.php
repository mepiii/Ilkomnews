<?php

// NOTE: This controller was intentionally disabled. IlkomNews is an admin-only
// portal; public self-registration has been removed (see routes/auth.php) and
// admins are created via the admin API (Api/Admin/AdminProfileController).
// The file is kept only as a tombstone so it never autoloads a live route;
// delete it once the deployment pipeline confirms no references remain.
