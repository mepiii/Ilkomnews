{
  "name": "ilkom-chatbot-service",
  "version": "1.0.0",
  "description": "Standalone multi-provider chatbot service for the ILKOM NEWS project (native TypeScript, direct provider SDKs / REST).",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "latest",
    "@google/genai": "latest",
    "axios": "^1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "^2"
  }
}
